import React from "react";

function Addlinks({...props}){
    return (
        <div className="Addlinks" {...props}>
            <button>Add link</button>
        </div>
    );
}

export default Addlinks;